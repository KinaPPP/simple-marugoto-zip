# Privacy

シンプルまるごとZIPは、SNS上でユーザーが閲覧できる投稿メディアをローカルへ保存するためのChrome拡張機能です。

## Bluesky β版の通信と保存

- 公開プロフィールのハンドルから、Bluesky公開API（`public.api.bsky.app`）でDID・投稿・添付情報を取得します。DIDからPDS情報を取得するため `plc.directory` 等の公開DID解決先へ接続します。
- 元のBlobの取得時だけ投稿者のPDSへHTTPS通信を行います。既定で許可されていないPDSは、収集開始時にChromeのアクセス許可を求めます。元画像がPDSから取得できない場合は `cdn.bsky.app` の表示用画像を試みます。
- ハンドル、DID、PDSのHTTPSオリジン、CID、投稿ID、メディアURL、収集・ZIP進捗を本拡張のローカルストレージへ保存します。公開APIの取得にユーザーのCookie・APIキー・パスワード・アクセストークンは使用しません。
- ZIP内とエクスポートJSONにも上記のアカウント別状態を必要な範囲で保存します。保存先フォルダへのアクセス権は移植しません。自分や他人の利用履歴を示し得るため、ZIP・JSONを公開する前に確認してください。

## 現在のX版について

- Xでログイン中のブラウザセッションを利用します。
- Xのアカウントパスワードを本拡張へ入力・保存する機能はありません。
- 収集・ZIPレジューム情報は `chrome.storage.local` に保存します。
- ZIPファイルはユーザーのローカル環境へ保存します。
- v0.0.21以降の各ZIPには、そのアカウントの収集状態を `backup-state.json` として含めます。設定画面から拡張全体の状態をJSONへ書き出したり、ZIP／JSONを読み込んで復元できます。
- バックアップは明示的な許可リストで生成し、APIキー、アクセストークン、Cookie、パスワード、任意の将来設定、ファイルシステムのアクセスハンドルは含めません。
- バックアップには、対象アカウント名、投稿ID、収集したメディアのCDN URL、画像・動画の種類、ZIP保存履歴が含まれます。保存先フォルダ名・アクセス権は含みません。個人の利用履歴を示し得るため、共有時は取り扱いにご注意ください。
- メディアURLは現在対応するX CDNのHTTPSホストのみを出力し、想定外のクエリパラメーターを削除します。メディア取得URLが無効になった場合は再収集が必要な場合があります。
- 別PCで復元しても、保存先フォルダのアクセス権・ログイン状態は引き継がれません。
- v0.0.22以降、設定ページの「すべて削除」（v0.0.23で改称）で、当該Chrome環境の本拡張の収集・保存状態、共通設定、保存先フォルダの記憶を削除できます。収集・ZIP処理中は実行できません。
- 全体初期化・拡張のアンインストールは、本拡張が過去に出力したローカルZIP・JSONファイルやChrome本体のログイン情報を削除する操作ではありません。既に別PCへ移行した状態にも影響しません。
- 本拡張独自の外部サーバーへ利用履歴、メディア、認証情報を送信する機能は実装していません。
- アクセス解析・広告SDKは実装していません。

## 将来のProvider

Threads等、公式APIのアクセストークンが必要なProviderを追加する場合は、保存する情報と用途を本ファイルへ追記します。パスワードそのものを保存する設計にはしません。

## 注意

SNS側の仕様や利用規約は変更される場合があります。保存対象について必要な権利・許可を確認した上で利用してください。
