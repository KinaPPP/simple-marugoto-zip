# Changelog

## v1.0.0 (2026-09-24)

- 初回正式版。v0.0.24の実機確認済みコードを変更せずにバージョンと公開用ドキュメントを整備。
- 対応SNSはX（旧Twitter）。手動収集を初期値とし、自動収集は任意で選択可能。
- 自動ZIP分割は300 MBまたは300ファイル。画像・動画の種類別ZIP、差分収集、収集状態のJSONバックアップ・復元に対応。
- 設定画面の疑似429ボタンと10ファイル分割選択は非表示。開発用429回帰テストと旧作業の互換処理は維持。
- 非公式ツール。Xの利用規則に注意し、アクセス制限が発生した場合は使用を中断してください。

## v0.0.24 (2026-09-23)

- v1.0.0直前のリリース候補。収集方法の初期設定を「手動」に変更し、自動収集は選択肢として継続。
- 設定タブから開発用「疑似429テスト」UIを削除。疑似429の回帰テストコードは開発者向けに維持。
- ZIP分割の「10ファイル」をUIから削除。自動分割を500 MB / 500ファイルから **300 MB / 300ファイル** の先着条件へ変更。
- v0.0.23以前の進行中10ファイル分割ジョブを再開できるよう、内部互換値のみ維持。


## v0.0.23 (2026-09-23)

- 設定ページの初期化欄を「シンプルまるごとZIPの拡張データを削除します。」と明記し、ボタンを「すべて削除」に短縮。ほかのChrome拡張まで削除する誤解と同じ文言の繰り返しを防止。
- 最終確認は従来どおり左「いいえ」／右「はい、削除する」。データ削除処理自体には変更なし。
- v0.0.22で刷新した青／緑アイコンを正式候補として維持。

## v0.0.22 (2026-09-23)

- 選定した画像＋動画→矢印→ZIPのアイコンを青／緑の2色系で描き直し。16pxは文字と細線を減らした専用図形にし、32/48/128pxとSVGソースも同梱。
- 設定ページをコンパクト化。ZIPまたはJSONをドロップするだけで未登録アカウントを自動追加し、既存アカウントはそのまま保持。重複がある場合だけ上書きを表示し、全体置換は詳細欄に収納。
- アカウント上書き・全体置換・全データ初期化は、必要なときだけ画面内の「いいえ／はい」で確認。疑似429診断は下部の折りたたみ欄へ移動。
- 全データ初期化でこのPCの拡張ストレージ、IndexedDBの保存先フォルダハンドル、アラームを初期化。実ファイルとChrome本体のログイン情報は対象外。収集中やZIP保存中は操作を拒否。
- 認証情報をバックアップへ含めないv0.0.21の許可リスト方式を継続。

## v0.0.21 (2026-09-23)

- すべての新規作成分割ZIPへ `ユーザー名/backup-state.json` を同梱。ZIPごとの保存済みチェックポイントと未保存の残りを区別し、DEFLATE圧縮する。
- 設定タブに全体JSONエクスポートとZIP／JSONからの復元（プレビュー、既存優先の追加、アカウント単位の置換、全体置換）を追加。
- 設定・メディア収集状態を明示的な許可リストで書き出し、APIキー・パスワード・Cookie・トークン・保存先フォルダ名・保存先ハンドルを対象外にする。
- ZIPを全件読み込まず、中央ディレクトリからJSONだけを探し、CRC検証・容量制限を行う。
- 現在のZIP処理・収集中の全体バックアップ／復元を安全のため制限する。保存先フォルダは復元後に再選択する。
- 新規ZIPの実機Chrome検証はこれから。


## v0.0.20 - 2026-09-23（実機確認待ち）

- 前回ZIP保存完了済みのXアカウントで「新規分をチェック」を追加。前回収集した最新Post IDをチェックポイントとして保存し、Xの `/media` を上から確認して新しい投稿のメディアだけを収集する。
- 自動／手動の両方で差分チェックに対応。前回の投稿に到達したら関連通信が落ち着くのを待ち自動終了。境界投稿が無い場合は、UserMediaの複数の古い投稿または自動モードの最下部到達で判定。
- 前回の収集・ZIP情報は差分チェック開始時に別キーへ退避。新規0件なら元の状態へ復元。停止中の差分は「前回に戻す」で取り消せる。
- 新規がある場合は新規メディアだけをZIP化し、前回の保存フォルダを可能なら再利用する。新規分をZIP保存し終わるまでは次回チェックを開始しない。
- 前回ZIP未保存の収集データを差分チェックで上書きしない安全ガードを追加。古い投稿IDはJavaScript Numberに丸めず文字列として比較。
- Xへの通信なしで差分検出／前回復元／安全停止／手動モードの境界自動終了を検証する回帰テストを追加。

## v0.0.19 - 2026-09-23

- Xの収集方法に「自動／手動」を追加。ポップアップの対象ユーザー直下で選択でき、設定は全アカウント共通で保持。固定高ポップアップには新しい段を増やさない。
- 手動モードでは拡張側からスクロールせず、Xの画面でユーザーがスクロールして発生した通信を受動収集。「収集を完了」はユーザーの明示操作にし、通信・保存処理中の誤完了を防止。
- 手動モードでも途中停止・収集再開・疑似429／通常429の待機・自動復帰（手動モードを維持）・ZIP保存を共通化。初回 /media 読み込み時の早着レスポンスを短期間保持して橋渡し準備後に再送。
- 収集中の別タブ／古い収集ジョブから遅れて届いたメディアは追加しない。手動収集の終了は元のタブとジョブIDが一致する場合だけ許可。
- 自動収集の最後の待機を「収集の最終確認中…」で明示。疑似429と実際の429の説明は中央ステータス領域へ統合し、緑ボタン上に重複していた通知帯を削除。
- 疑似429の既存回帰テストに、手動モード・タブ照合・明示的完了・モード維持・初回レスポンスの再送テストを追加。

## v0.0.18 - 2026-09-23

- 設定画面の「疑似429テスト」から、進行中のX収集へテスト信号を送れるようにした。ポップアップの高さと通常UIは変更なし。
- 疑似429はXのHTTP通信を改変せず、実際の429検出と同じcontent→backgroundの待機処理へ合流する。既定の自動再開待ち時間は90秒。
- 疑似429と実際のXの429をステータス欄・ツールバーホバー・保存状態で明確に区別する。
- 設定画面以外からの疑似429操作と、別ジョブ・別タブ・収集終了後への誤注入を拒否する。
- 疑似429中の収集データを維持し、通常の再開アラームで既存の継続・重複排除へ戻る。Xへのアクセスを増やさずに待機UIを確認できる。
- 本体とテスト用モックの動作を分け、実際の429と疑似429のフローをテストできる回帰スクリプトを追加。

## v0.0.17 - 2026-09-22

- X `/media` 収集を固定約1.8秒周期の自動スクロールから、`UserMedia` / `UserTweets` / `SearchTimeline` の関連通信開始・終了を監視するレスポンス待ちペーシングへ変更。
- 関連通信が処理中の間は次のスクロールを送らず、レスポンス解析後も短いsettle時間を置いてから進むよう改善。
- 前回スクロール後に関連通信が発生しない場合だけ低頻度で最下部を再確認し、不要な連続スクロールを抑制。
- 収集完了判定に「関連通信が一定時間落ち着いている」条件を追加し、遅いレスポンス処理中の早すぎる完了判定を防止。
- 429検知・手動停止・再開の既存処理は維持し、将来の手動スクロール収集モードへ分離しやすいよう通信状態の監視を独立化。

## v0.0.16 - 2026-09-22

- ZIP保存完了時の緑アイコン＋ `✓` を短時間で消さず、ユーザーが拡張アイコンを押してポップアップを開くまで保持するよう変更。
- 別タブ・別アカウントへ移動しても未確認のZIP完了通知を維持し、バックグラウンド作業の完了を見逃しにくくした。
- ポップアップを開いた時、または次の収集／ZIP処理を開始した時に、以前の完了通知を確認済みとして解除するよう変更。
- Service Worker再起動後も、未確認の完了通知をツールバーへ復元するよう改善。
- 完了時のホバー表示に保存したZIP数と、取得できる場合は保存先フォルダ名を追加。

## v0.0.15 - 2026-09-21

- 大量件数の「確」バッジを確認対象のXアカウント／タブに紐付け、別アカウントへ移動した時に引き継がれないよう改善。
- タブ切替・URL変更・ウィンドウ切替時にツールバー状態を再評価するよう変更。
- ZIPファイル名を `ユーザー_YYYYMMDD_HHmmss_media/images/videos_連番.zip` に変更。
- 画像のみ／動画のみ／両方を別ジョブとして分かりやすく管理できるよう改善。
- ZIP完了後に画像／動画の選択を切り替えた場合、件数大小に関係なく「新規にZIPで保存」と判定するよう修正。

## v0.0.14

- 分割ZIPの連続保存中、保存先ハンドルを毎回IndexedDBから再確認せず、同一実行中はメモリ上で再利用するよう変更。
- IndexedDBから復元した保存先が `prompt` 状態の場合、ZIP保存ボタン押下時のユーザー操作中に `requestPermission()` で再許可を試みるよう改善。
- 10ファイル分割などで序盤に何度も「保存先フォルダを再選択してください」と一時停止する問題を修正。
- OS/Chrome側で権限が実際に失われた場合は従来どおり安全に一時停止し、再選択へ誘導する。

## v0.0.13

- 1000件以上の大量メディア警告をXページ側の `window.confirm()` から、拡張ポップアップのステータス領域内確認UIへ変更
- 「進捗をリセット」のブラウザ標準確認ダイアログを廃止し、同じステータス領域内で確認する方式へ統一
- 確認UIは左にキャンセル系、右に実行系を配置。通常実行は青、破壊的なリセットだけ赤で表示
- 大量件数警告で「続ける」を押した場合、同じXタブで警告を再表示せず収集を開始するようにした
- 大量件数の確認待ち中はツールバーに黄色の `確` バッジを表示し、ポップアップが閉じても確認待ちだと分かるようにした
- ZIP保存完了時、選択した保存先フォルダ名をステータス領域へ表示
- 保存先フォルダをOSのExplorerで自動表示する機能は、Chrome拡張APIの制約を考慮して不採用

## v0.0.12

- 大きなZIPを保存先フォルダへ書き込む際、全ファイルを一度にメモリへ保持せず、File System Access APIへ順次ストリーミング書き込みする方式へ変更
- 500ファイル分割などで90%前後まで進んだあとZIPが作成されず「ZIP保存を再開」を繰り返す問題を修正
- 保存中に失敗した未完成ZIPは可能な範囲で削除し、再開時に同じ区間から安全にやり直すよう改善
- ZIP32の制限を超える破損ZIPを作らないよう、約3.75GBを内部安全上限として自動分割
- v0.0.11で追加した「10ファイル」動作確認用ZIP分割を継続


## v0.0.11

- ZIP分割に「10ファイル」を追加（動作確認用）
- 連番ZIPの保存先1回指定を少数ファイルで確認しやすくした
- 選択した10ファイル分割設定も他のユーザー設定と同様に記憶

## v0.0.10

- ZIP保存中のツールバーバッジを1%単位の数字表示へ変更。`%`記号は付けず、2桁までにして視認性を改善。
- ZIP保存完了時は3桁の `100` を表示せず、緑アイコン＋ `✓` を短時間表示してから待機時の青アイコンへ戻す。
- X `/media` 上部の「○件の画像と動画」を取得できた場合、参考総数として収集状態へ保存。
- X表示件数が1000件以上の場合、収集開始前に長時間処理の確認を表示。件数取得に失敗した場合は従来どおり収集を続行。
- File System Access API対応環境で、ZIP保存開始時に保存先フォルダを1回選び、分割ZIPを同じフォルダへ連続保存する実験機能を追加。
- 選択した保存先フォルダのハンドルをIndexedDBへ保持し、5ファイル動作確認後のレジュームでも再利用。
- 保存先権限が失われた場合は安全に一時停止し、次回のZIP保存操作でフォルダを再選択できるようにした。
- File System Access APIが利用できない環境では従来の `chrome.downloads` 方式へフォールバック。

## v0.0.9

- 画像/動画、ZIP分割、ダウンロードファイル数の選択を `chrome.storage.local` にユーザー設定として保存。
- 初回のみ `5ファイル（動作確認）` を既定値とし、一度変更した設定は次回以降も維持。
- ユーザー設定とアカウント別の収集・ZIP進捗を別データとして管理。
- 待機中は過去の `archive_error` をツールバーの赤い `!` として復元しないよう変更。
- v0.0.7以前に残った `USER_CANCELED` エラーを、安全なZIP一時停止状態へ自動移行。
- ポップアップ表示時にツールバー状態を再同期し、実際に処理中でなければ青アイコンのみへ戻す。

## v0.0.8
- 待機中・停止中・完了済みの古い状態でツールバーが緑やバッジ表示のまま残る問題を修正。実際に処理中でない場合は青アイコンのみへ戻す。
- Service Worker再起動時、実際のoffscreen ZIPジョブが存在しない `archiving` 状態を安全な一時停止へ自動修復。
- ZIP保存中のツールバーバッジを `%` 付き毎1%更新から、`0 / 10 / 20 ... 90` の10%刻み数字表示へ変更。小さいアイコンでの見切れとホバー表示の頻繁な消失を軽減。
- ZIP保存中のホバー文言も10%刻みにして更新頻度を低減。
- 保存ダイアログのキャンセル（`USER_CANCELED`）をエラーではなく安全なZIP一時停止として扱うよう変更。
- 小さい「収集を再開」ボタンを廃止。収集再開は上部の大きな青ボタンへ一本化。
- 小ボタン「最初から」を「進捗をリセット」へ変更。リセットは保存済みの収集/ZIP進捗を消すだけで、再収集は自動開始しない。
- 収集完了時の大きな青ボタンは「収集完了」として無効化し、再取得したい場合は進捗リセット後に改めて開始する流れへ整理。
- ZIP処理中はリセット操作も無効化し、競合操作を防止。
- 通知トーストの位置を調整し、最下部のZIPボタンとの重なりを軽減。

## v0.0.7
- Chromeツールバーの拡張アイコンを状態表示として利用する機能を追加。
- 通常時は青アイコン、ZIP保存中は緑アイコンへ切り替える。
- 収集中は `収`、429待機は `待`、ZIP中は進捗 `%`、再開待ちは `続`、完了は `✓`、エラーは `!` をバッジ表示。
- ツールバーアイコンのホバー文言に、収集件数・ZIP進捗・処理件数を表示。
- Service Worker再起動後も、進行中または再開待ち状態をツールバーへ復元する処理を追加。
- ZIP保存中は収集ボタン・収集停止・レジューム・メディア種別・ZIP分割・デバッグ件数をロックし、競合操作を防止。
- 通常用の青アイコンとZIP処理用の緑アイコンを同梱。

## v0.0.6
- ZIPファイル名を `ユーザー名_YYYYMMDD_HHmm_連番.zip` 形式へ変更。
- 同じZIP保存ジョブをレジュームした場合は、最初の保存開始日時を維持して連番だけを進める。
- Blob URL由来のUUID名で保存される環境向けに `downloads.onDeterminingFilename` で保存名を再指定する処理を追加。
- ZIP作成中のステータス表示にも実際の予定ファイル名を表示。

## v0.0.5
- 実地テストで、5ファイル単位のZIPレジュームが正常に動作することを確認。
- ポップアップ全体をスクロールなしの固定レイアウトのまま 590px 高へ微調整。
- ステータス欄を 108px に拡張し、「収集を再開」「最初から」ボタンが見切れないように修正。
- 429待機やレジューム説明など、2行メッセージ＋操作ボタンを常時収められる余裕を確保。

## v0.0.4
- 収集を手動停止した時点までの画像・動画をZIP保存できるように変更。
- 収集状態とZIP保存状態を分離し、途中ZIP保存後も同じ収集作業をレジューム可能にした。
- 収集ジョブに `collectionId` を追加し、同じ作業内でZIP進捗を継続できるようにした。
- ポップアップを固定高・スクロールなし前提で再設計。
- ボタン、余白、説明文を圧縮し、主要操作を1画面に収めた。
- ステータス欄の高さを固定し、長いメッセージでレイアウトが伸びないようにした。
- 通知はオーバーレイ表示に変更し、ポップアップ全体の高さに影響しないようにした。
- 5ファイル・デバッグ保存とZIPレジュームを継続。

## v0.0.3
- 既に開いているXタブへcontent scriptが存在しない場合、自動注入して再接続する処理を追加。

## v0.0.2
- 5ファイル単位のデバッグ保存を追加。

## v0.0.1
- X向け初期テスト実装。
