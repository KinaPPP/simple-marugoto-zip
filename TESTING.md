# v1.0.0 実機・回帰チェック

新しいPC／データ初期化後の環境で、手動収集が最初から選択されていることを確認する。既存環境で以前から自動を選んでいた場合は、その設定が維持されるのが正しい。

1. 設定タブに疑似429の操作欄が表示されないこと。疑似429回帰テストは `node tests/simulated-429.test.cjs` でオフライン実行する。
2. ZIP分割が「自動 / 500 MB / 1 GB / 500ファイル」の4択で、説明が「300 MBまたは300ファイル」となっていること。
3. 既存の少量アカウントで、手動収集→「収集を完了」→ZIP保存ができること。実際のXで300件・300 MBを用意する必要はない。自動分割の閾値は `node tests/release-candidate.test.cjs` で検証する。
4. v0.0.23で10ファイル分割中に停止した作業があれば、自動が選ばれた状態で「ZIP保存を再開」して元の10ファイル単位で続くことを確認する。古いジョブがなければこの項目は省略してよい。

---

以下は以前のバージョンのテスト履歴です。**v1.0.0の画面には疑似429操作欄と10ファイル分割は表示されません。** 旧機能のテスト手順はそのまま履歴として残しています。

## v0.0.23 設定UI・正式アイコン候補・全データ初期化テスト

1. 既存の拡張を**削除せず**v0.0.23へ上書き更新。通常青アイコン、ZIP中の緑アイコン、16pxでの見え方と黄「確」・緑「✓」の視認性を確認。
2. 設定画面はZIP/JSON復元と全体JSON保存が上部に並ぶ。大きな説明段落が無く、疑似429の診断欄は下部へ折りたたまれていることを確認。
3. 既存PCから全体JSONをエクスポート。未導入PCの設定画面へドラッグ＆ドロップすると、新規アカウントが**確認なしで自動追加**されて「追加件数」が表示されることを確認。保存先フォルダは次回ZIP保存時に再選択。
4. 同じJSONをもう一度読み込むと「0件追加・登録済みn件」と表示し、既存収集件数とZIP進捗が変わらないことを確認。必要な場合だけ「同じアカウントを上書き」→「いいえ／はい、置き換える」を確認。
5. 全体JSONの全体置換は「詳細な復元操作」内にだけある。ZIPでは全体置換ボタンを表示しない。誤クリックで勝手に上書きしないことを確認。
6. 全体初期化はテスト用PCで実施すること。実行前に必要ならJSONをエクスポートし、「すべて削除」→**いいえ**でキャンセルするとデータ維持。「はい、削除する」を選ぶと拡張が初期状態に戻ることを確認。保存済みZIP/JSON自体は残り、次回ZIP保存時にはフォルダを選び直す。
7. 収集中またはZIP保存中はバックアップ復元と全体削除がエラーで拒否されることを確認。

オフラインテスト: `node tests/backup-restore.test.cjs` と `node tests/options-reset.test.cjs`。

# v0.0.21 ZIP内状態ファイル・全体バックアップ移行テスト（別PC）

1. 既存PCのv0.0.20を**拡張削除せず**v0.0.21へ更新。収集／ZIP作成が止まっている状態で設定タブ上部の「全体JSONをエクスポート」。既存のアカウント数・選択設定が含まれることを確認。APIキー・トークン・Cookie・パスワードは含まれません。
2. 未導入PCにv0.0.21を読み込み、設定タブへ全体JSONをドロップ。内容のプレビュー後、既定の「既存を残して追加」で復元。アカウントページを開いてポップアップを確認し、収集件数・差分基準点・ZIP進捗・画像/動画選択が元と一致することを確認してください。**初回のZIP保存時には保存先フォルダを選び直します。**
3. ZIPからの個別復元を試す場合は、既存PCでv0.0.21を使って小規模アカウントのZIPを新規作成。v0.0.20以前のZIPではこの機能を試せません。
4. 「10ファイル＋無制限」で11件以上を保存し、`_001.zip` と `_002.zip` の中に `ユーザー名/backup-state.json` があることを確認。先頭ZIPからだけ復元した場合は**次の11件目からZIPを再開**できること、最後のZIPはZIP保存完了になることを確認。ZIPの「10ファイル」はメディア数なので、JSON込みの内部ファイル数は11です。
5. 同じアカウントが既に存在する場合、既定の復元は**既存データを変更せずスキップ**することを確認。必要なら既存PCからJSONを別途退避したうえで「同じアカウントを置き換える」を選択します。
6. ZIPそのものはPC間で自動転送されません。後半のZIPから状態を復元する場合、**前半のZIPが実際に手元に揃っているかも確認**してください。ファイルの存在は自動検証されません。

オフラインテスト: `node tests/backup-restore.test.cjs`, `node tests/delta-collection.test.cjs`, `node tests/simulated-429.test.cjs`。別PCへの本番データ移行は、オフライン試験とは別の実機確認事項です。

---

# v0.0.20 差分収集の実機テスト（前回バックアップ済みアカウント）

1. v0.0.19以前に収集・ZIP保存を完了したXアカウントを開き、同じ拡張をv0.0.20へ更新します。**テスト前にChromeの拡張データをリセットしないでください**。前回ZIP未保存のアカウントでは新規分チェックを無効にします。
2. 前回から投稿が増えているアカウントなら、まず「手動」を選んで「新規分をチェック」。`/media` を上からスクロールして、前回の最新投稿が現れた位置で自動終了することを確認。新規件数が正しく表示され、以前の収集分が混ざっていないか確認します。
3. 今回の差分だけをZIP保存し、ファイル名／連番／保存先フォルダの再利用を確認。新規分を画像のみ・動画のみで別々に保存した場合、両方が済むまで次のチェックを開始できないこと。
4. 同じアカウントでもう一度「新規分をチェック」を実行。追加投稿がなければ「新規メディアはありません」と表示され、前回ZIP情報が維持されること。
5. 追加テスト: チェック中に「停止」を押してから「前回に戻す」で差分を取り消し、元の収集件数・ZIP情報が戻ること。別アカウントの収集状態に影響しないこと。
6. 自動モードでも同じ小規模テストを行う。境界が見つからない場合に過早に「新規0件」にならないことを確認。大量アカウントは最初のテストに使わないでください。
7. 旧版の画像のみ／動画のみの別保存履歴を自動判別する機能はありません。必要な種類を保存済みか確認してからテストしてください。

オフライン回帰テスト: `node tests/delta-collection.test.cjs` および `node tests/simulated-429.test.cjs`。

---

# v0.0.19 初回テスト手順

まず小さめのXアカウントで試してください。

1. ChromeでXへログイン。
2. 対象ユーザーのプロフィールを開く。
3. 「シンプルまるごとZIP」を開き、対象が `@ユーザー名` になっていることを確認。
4. 「メディアを収集」を押す。
5. `/media` へ移動または再読み込み後、ページが自動的に下へスクロールすることを確認。
6. ポップアップを再度開き、画像 / 動画件数が増えているか確認。
7. 途中で「停止」し、「続きから再開」で再開できるか確認。
8. 収集完了後、まずは「自動」「画像＋動画」でZIP保存。
9. ZIP内部のフォルダとファイル名を確認。

## v0.0.19 手動スクロール収集テスト

1. まず数十件〜200件程度のアカウントを開き、ポップアップの対象ユーザーの下にある「収集方法」で「手動」を選択する。次にポップアップを閉じて開き直しても設定が記憶されることを確認。
2. 「メディアを収集」を押すと `/media` へ移動／再読み込みされる。初回読み込み後、**拡張が勝手に下へスクロールしないこと**を確認。
3. Xで自分の手で下へスクロールし、必要な範囲を表示する。スクロール時にはポップアップが閉じるが、Xタブに残る収集処理は止まらない。再び拡張アイコンを押したとき、画像・動画件数が増えていること。止まったまま放置しても自動完了しないこと。
4. 収集を終えたい位置までスクロールしたら、**同じXタブで拡張アイコンを再度クリック**する。収集済みが1件以上なら、青いボタンが「収集を完了」になる。読み込みが落ち着いてから押すと通常の「収集完了」に変わり、緑の「ZIPで保存」が押せること。途中でやめて後日続きを読みたい場合は、右側の「停止」を選び、次回「収集を再開」を使う。
5. 別の小さいアカウントで手動収集→途中「停止」→「収集を再開」を試す。件数の消失・重複がないこと。最後は明示的に完了してZIP保存する。
6. 【追加テスト】手動収集中に疑似429を送信し、90秒後に自動復帰しても **手動のまま** であり、自動スクロールが突然始まらないこと。
7. 【追加テスト】手動収集中に別タブで同じアカウントを開いても、意図せず両方で同一収集ジョブが進まないこと。
8. 自動モードへ戻した場合は従来のレスポンス待ち自動スクロールと最終確認表示が動作すること。

最初の検証は小規模アカウントのみで十分です。XのWeb仕様・利用規則は変更される場合があり、手動スクロールであっても拡張のレスポンス監視が許可されることは保証しません。

## v0.0.18 疑似429・待機・再開テスト（小規模アカウント）

1. 数十〜数百件の対象Xアカウントで収集を開始し、`/media` 画面がスクロール中であることを確認する。
2. 設定タブ（⚙）を開き、「疑似429テスト」欄の「収集中の状態を更新」を押す。該当アカウント・途中の収集数が表示されること。
3. 「この収集に疑似429（90秒）」を1回押す。実際のHTTPリクエスト/レスポンスは改変されない。
4. 対象Xタブで収集が止まり、ツールバー青＋黄 `待`、ポップアップで「疑似429テスト：一時停止中」と約90秒後の予定時刻が見えること。
5. 既存収集データが消えないこと。90秒間は収集の青ボタンが「待機中」で無効のこと。
6. 対象Xタブを維持したまま90秒後、通常の自動再開が始まること。再開後に収集数が初期化されず、同じメディアが二重登録されないこと。
7. **追加テスト（別途実施）**: 再度疑似429を送り、待機中に「停止」を押す。自動再開アラームが解除され、保存済み件数のまま「収集を再開」から手動復帰できること。
8. **追加テスト（別途実施）**: 疑似429待機中にXの `/media` タブを閉じた場合、再開時刻後は「一時停止」のままになり、ユーザーがページを開いて明示的に再開できること。
9. 未開始・別アカウント・収集完了の状態では、疑似429を誤適用しないこと。実際の429はこれとは別物であり、本番のHTTP 429が発生した場合だけ別途挙動を記録する。

Xへの不必要なアクセスを避けるため、疑似429の発生回数をむやみに増やす必要はありません。コード側のオフライン検証は `node tests/simulated-429.test.cjs` で可能です。

## 問題が起きたら

Chromeの `chrome://extensions/` → 本拡張 → 「Service Worker」を開き、Consoleのエラーをコピーしてください。

X側の取得問題の場合は、対象Xページで DevTools Console に `[Simple Marugoto ZIP]` 相当のエラーがないかも確認してください。


## 5ファイル・レジューム確認

1. 「ダウンロードファイル数」は初期値の **5ファイル（動作確認）** のままにします。
2. 収集完了後に「ZIPで保存」を押します。
3. ZIPに最大5ファイルだけ入っていることを確認します。
4. ポップアップを開き直し「ZIP保存を再開」を押します。
5. 次の最大5ファイルが保存され、前回と重複しないことを確認します。

## 接続回帰テスト

1. Xのユーザーページを先に開いたままにする。
2. `chrome://extensions/` でシンプルまるごとZIPを「再読み込み」する。
3. Xタブは手動リロードせず、そのまま拡張ポップアップを開く。
4. 「メディアを収集」を押す。
5. `Receiving end does not exist` が出ず、自動的に `/media` へ移動して収集が始まれば成功。


## 途中停止→ZIP保存テスト
1. メディア収集を開始し、件数が増えたところで「停止」。
2. 「ZIPで保存」が有効になることを確認。
3. 動作確認の「5ファイル」を選んだまま保存し、ZIPに5ファイル入ることを確認。
4. 再度「ZIP保存を再開」で6〜10件目が別ZIPに入ることを確認。
5. 「収集を再開」でXの収集自体も続きから再開できることを確認。

## UIテスト
- 自動／手動ラジオの追加後も、通常表示・収集中・停止中・ZIP作成中のいずれでもポップアップ自体に縦スクロールバーが出ないこと。
- ステータスメッセージが2行を超えてもポップアップの高さが変化しないこと。


## ZIPファイル名の確認

5ファイル保存を2回実行し、同じ作業内で以下のように日時部分が共通、連番だけが進むことを確認してください。

```text
ppp200x_20260919_230500_media_001.zip
ppp200x_20260919_230500_media_002.zip
```

UUID形式のファイル名にならないことも確認してください。


## ツールバー状態表示テスト（v0.0.12）

1. 待機中はツールバーの拡張アイコンが青で、バッジが付いていないことを確認。
2. 「メディアを収集」を開始し、青アイコン + `収` バッジになることを確認。
3. アイコンへマウスを重ね、`メディア収集中 @ユーザー名` と検出件数が表示されることを確認。
4. 収集を停止し、バッジが `休` になることを確認。
5. 「ZIPで保存」を押し、アイコンが緑に変わり `0`〜`99` の進捗数字が更新され、完了時に緑の `✓` が表示され、ポップアップを開くまで保持されることを確認。
6. 保存ダイアログでポップアップが閉じても、緑アイコンと進捗バッジが更新され続けることを確認。
7. ZIP保存中にポップアップを開き直し、青い収集ボタン、停止、レジューム、画像/動画チェック、ZIP分割、動作確認件数が操作不可になっていることを確認。
8. ZIP完了時に緑アイコン + `✓` になることを確認。5ファイルで一時停止した場合は完了通知ではなく再開可能な状態になることを確認。

### ZIP保存中のタブ移動テスト

1. メディア収集を完了または手動停止する。
2. 「ZIPで保存」を開始する。
3. ZIP作成開始後に別タブへ移動、または元のXタブを別ページへ移動する。
4. ツールバーの進捗バッジが動き続け、ZIP保存が完了することを確認。

※メディア**収集中**はXの `/media` ページを使用しているため、収集中に対象ページから移動すると安全のため収集を停止します。


## v0.0.12 追加確認

- 何も処理していない時は青アイコンのみになること。
- ZIP保存中は緑アイコン＋1%単位の数字になること。`%` 記号は表示せず、完了時のみ `✓` を表示する。
- ZIP保存中にポップアップを開き、収集・リセット・設定項目が操作できないこと。
- 保存ダイアログをキャンセルした時、`USER_CANCELED` の赤いエラー表示にならず「ZIP保存をキャンセルしました」と一時停止すること。
- 収集途中では大きな青ボタンが「収集を再開」になり、小さい青い再開ボタンが存在しないこと。
- 「進捗をリセット」を押した場合はデータだけ消え、再収集は自動開始されないこと。

## v0.0.12 保存先1回指定テスト

1. 収集済みアカウントでZIP分割を「500ファイル」等にし、複数ZIPが作られる条件を用意します。
2. 「ZIPで保存」を押し、保存先フォルダを1回だけ選択します。
3. `001.zip` 保存後に `002.zip` 以降のフォルダ選択ダイアログが繰り返し出ないことを確認します。
4. 5ファイル（動作確認）で一旦停止した場合、もう一度「ZIP保存を再開」を押しても同じフォルダへ続けて保存されることを確認します。
5. ZIP完了時、ツールバーが緑＋ `✓` のまま保持され、拡張アイコンを押してポップアップを開いた時に青アイコンへ戻ることを確認します。

## v0.0.12 大量件数警告テスト

- `/media` 上部の件数が1000件以上のアカウントで、収集開始時に長時間処理の確認が出ることを確認します。
- 「キャンセル」を選んだ場合、スクロール収集を開始せず待機状態になることを確認します。
- 1000件未満、または件数を取得できない場合は警告なしで従来どおり開始することを確認します。

## 10ファイル分割テスト

1. ZIP分割を「10ファイル」にします。
2. 21件以上の収集結果でZIP保存を開始します。
3. `_001.zip` と `_002.zip` が10ファイルずつ、残りが `_003.zip` に保存されることを確認します。
4. 保存先フォルダを最初に1回だけ選択した場合、2個目以降で保存ダイアログが出ないことを確認します。

## v0.0.12 大容量ZIP回帰テスト

1. 100件以上のメディアを収集する
2. 「ダウンロードファイル数」を「無制限」にする
3. ZIP分割を「10ファイル」で試し、複数ZIPが同じ保存先へ連続保存されることを確認する
4. 次にZIP分割を「500ファイル」にして、500件前後まで進んだところで処理がループせずZIPファイルが生成されることを確認する
5. 保存完了後、ツールバーが緑＋ `✓` を保持し、ポップアップを開いた後に青の待機状態へ戻ることを確認する


## v0.0.15 確認UIテスト

1. Xの `/media` 表示件数が1000件以上のアカウントで「メディアを収集」を押します。
2. Xページ側の標準confirmが出ず、ポップアップのステータス領域に大量件数警告が表示されることを確認します。
3. 左の「キャンセル」で収集を開始せず停止状態へ戻ることを確認します。
4. もう一度開始し、右の「続ける」で警告を再表示せず収集が始まることを確認します。
5. 収集を停止し「進捗をリセット」を押した時も別ウィンドウが出ず、ステータス領域内に確認が出ることを確認します。
6. 左の「やめる」で元の状態へ戻り、右の赤い「リセット」で進捗だけが消えることを確認します。

## 保存先名表示テスト

1. File System Access APIの保存先フォルダを選び、ZIP保存を最後まで完了します。
2. 「ZIP保存完了」の詳細に `保存先: フォルダ名` が表示されることを確認します。
3. Windowsの絶対パスやフォルダ自動オープンは行われないことを確認します。


## v0.0.15 保存先権限の回帰テスト

1. 50件以上収集できるアカウントで「無制限」「10ファイル」を選びます。
2. 最初の1回だけ保存先フォルダを選択します。
3. `_001.zip` から複数ZIPが、途中で保存先再選択を求められず同じフォルダへ連続保存されることを確認します。
4. 動画のみ保存完了後、画像のみに切り替えて「新規分をZIPで保存」を実行し、権限が有効な限り同じ保存先をそのまま利用できることを確認します。
5. Chrome側でフォルダ権限を明示的に取り消した場合だけ、安全に「保存先フォルダを再選択してください」へ移行することを確認します。


## v0.0.15 追加確認

### 大量件数確認の対象切り替え

1. 1000件以上のアカウントで「確」表示まで進める。
2. 同じタブで別アカウントへ移動する。
3. 黄色い「確」が消えることを確認する。
4. 元のアカウントへ戻り、確認UIと「確」が復元されることを確認する。

### ZIPファイル名

- 画像＋動画: `handle_YYYYMMDD_HHmmss_media_001.zip`
- 画像のみ: `handle_YYYYMMDD_HHmmss_images_001.zip`
- 動画のみ: `handle_YYYYMMDD_HHmmss_videos_001.zip`
- 同じジョブの連番では日時と種類名が固定されること。
- 画像のみ→動画のみ等へ切り替えた場合、新しい日時・種類・001から始まること。

## v0.0.16 完了チェック保持テスト

1. 複数ZIPが生成される条件で「無制限」でZIP保存を開始します。
2. ZIP処理中に別タブへ移動し、処理完了まで拡張ポップアップを開かず待ちます。
3. 完了後、ツールバーが緑アイコン + `✓` のまま保持されることを確認します。
4. 別タブ／別Xアカウントへ移動しても `✓` が消えないことを確認します。
5. 拡張アイコンを押してポップアップを開くと、`✓` が解除され、その時点の通常ツールバー状態へ戻ることを確認します。
6. 対象アカウントのポップアップでは、`✓` 解除後もステータス領域に「ZIP保存完了」と保存件数／保存先フォルダ名が残ることを確認します。
7. 完了通知を開かないまま次の収集またはZIP保存を開始した場合、古い `✓` が自動解除され新しい処理状態へ切り替わることを確認します。
8. Chrome拡張のService Workerが再起動しても、未確認の `✓` が復元されることを確認します。



## v0.0.17 レスポンス待ちペーシングテスト

目的: Xの関連通信が処理中の間に次の自動スクロールを重ねず、通信完了後に次へ進むことを実機で確認する。

1. 数十〜数百件程度のテスト可能なアカウントで「メディアを収集」を開始する。
2. `/media` が下へ進むこと、画像/動画の件数が増えることを確認する。
3. v0.0.16以前のような固定約1.8秒周期ではなく、読み込みが発生した箇所ではレスポンス待ちに応じてスクロール間隔が伸びることを確認する。
4. X側の読み込みが遅い場面でも、同じタイミングでスクロール要求を連続して重ねないことを確認する。
5. 最下部付近では通信が発生しなくなっても即座に完了せず、低頻度の再確認後に「収集完了」へ移ることを確認する。
6. 途中で「停止」→「収集を再開」しても収集済み件数が保持され、重複なく続行できることを確認する。

速度より安定性を優先したため、最下部の完了判定は以前より数十秒長く見える場合がある。これは想定内。
